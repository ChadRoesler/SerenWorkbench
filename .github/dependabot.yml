version: 2
updates:
  # GitHub Actions used in .github/workflows/*. Dependabot rewrites the pinned
  # SHA (keeping the `# vX.Y.Z` comment in sync) when the action releases.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "ci"

  # Python deps for the workbench package (pyproject lives in SerenWorkbench/).
  - package-ecosystem: "pip"
    directory: "/SerenWorkbench"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "deps"

  # Node deps for the VS Code extension.
  - package-ecosystem: "npm"
    directory: "/SerenCorpusCallosumVSCode"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "deps(vscode)"